server:
  port: 8081

spring:
  application:
    name: trainee-supplier-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"